---
glyph: '∡'
pattern: 'r←x∡y'
name: Arctangent
---

`r` is the result of the dyadic arctangent (`atan2`) of `x` and `y`, where (confusingly) `x` is the $y$ coordinate and `y` is the $x$ coordinate: `r←∡y⊕x`