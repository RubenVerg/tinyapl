---
glyph: '∡'
pattern: 'r←x∡y'
name: Arctangent
planned: true
---

`r` is the result of the dyadic arctangent (`atan2`) of `x` and `y`, where `x` is the $y$ coordinate and `y` is the $x$ coordinate: `r←∡y⊕x`