---
glyph: '⍛'
pattern: 'r←[x](F⍛G)y'
name: Before
---

If `x` is not provided, `r` is `G (F y)`; if `x` is provided, `r` is `(F x) G y`.

|Monad|Dyad|
|:----|:---|
|<combinator key="reverse_compose" name="Reverse Compose" />|<combinator key="before" name="Before" />|