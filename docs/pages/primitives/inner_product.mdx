---
glyph: '∙'
pattern: 'r←x(F∙G)y'
name: Inner Product
---

`F` should be a reduction function. `r` is the result of the generalized inner product of `x` and `y`: combine major cells of `x` with minor cells of `y` using `G` and reduce the result using `F`: `r←x F⍤(G⍤0⍤¯1)⍤1‿99 y`.