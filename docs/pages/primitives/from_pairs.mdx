---
glyph: '߹'
pattern: 'r←߹y'
name: From Pairs
---

`r` is the dictionary with entries taken from `y`, which is a nested vector containing either two-element vectors (as created by&nbsp;<primitive key="pair" />), or (singleton) dictionaries (as created by&nbsp;<primitive key="key_value_pair" />).