---
glyph: '⌊'
pattern: 'r←x⌊y'
name: Minimum
---

`r` is the result of the scalar application of the minimum function, $\min(x, y)$, which returns the smaller argument as described by [total array ordering](/info/ordering).