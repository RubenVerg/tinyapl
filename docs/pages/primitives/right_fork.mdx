---
glyph: '⇽'
pattern: 'r←[x](F⇽G)y'
name: Right Fork
---

If `x` is not provided, `r` is `y F (G y)`; if `x` is provided, `r` is `x F (x G y)`.

|Monad|Dyad|
|:----|:---|
|<combinator key="right_hook" name="Right Hook" />|<combinator key="right_fork" name="Right Fork" />|