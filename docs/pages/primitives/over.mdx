---
glyph: '⍥'
pattern: 'r←[x](F⍥G)y'
name: Over
---

If `x` is not provided, `r` is `F (G y)`; if `x` is provided, `r` is `(G x) F (G y)`.

|Monad|Dyad|
|:----|:---|
|<combinator key="compose" name="Compose" />|<combinator key="over" name="Over" />|