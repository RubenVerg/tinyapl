---
glyph: '⇾'
pattern: 'r←[x](F⇾G)y'
name: Left Fork
---

If `x` is not provided, `r` is `(F y) G y`; if `x` is provided, `r` is `(x F y) G y`.

|Monad|Dyad|
|:----|:---|
|<combinator key="left_hook" name="Left Hook" />|<combinator key="left_fork" name="Left Fork" />|