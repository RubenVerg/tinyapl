---
glyph: '⍉'
pattern: 'r←⍉d'
name: Inverted Table
---

`r` is the inverted table constructed from dictionary `d`, i.e. a two-element nested vector containing a vector of the keys of `d` and a vector of the values of `d`.