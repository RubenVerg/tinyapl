---
glyph: '⍳'
pattern: 'r←x⍳y'
name: Index Of
---

`r` is the index of cells of `y` in the corresponding major cells of `x`. If a cell of `y` does not appear in the cells of `x`, the corresponding element of `r` is one more than the tally of `x`.