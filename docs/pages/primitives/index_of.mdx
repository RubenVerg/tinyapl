---
glyph: '⍳'
pattern: 'r←x⍳y'
name: Index Of
planned: true
---

`r` is the index of major cell of `y` in the corresponding cells of `x`. If a cell of `y` does not appear in the cells of `x`, the corresponding element of `r` is one more than the count of the cells of `x`.