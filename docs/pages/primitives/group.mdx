---
glyph: '⊆'
pattern: 'r←x⊆y'
name: Group
planned: true
---

`x` is a vector of naturals, with length equal to the tally of `y`. `r` is a nested vector containing major cells of `y`, in the positions marked by corresponding entries in `x`. Cells that correspond to a `0` are dropped from `r`.