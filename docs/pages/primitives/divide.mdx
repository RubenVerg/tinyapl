---
glyph: '÷'
pattern: 'r←x÷y'
name: Divide
---

`r` is the result of the scalar application of the division function, $\frac x y$. Fails if `y` is `0`, unless `x` is also `0` in which case the result is `1`.