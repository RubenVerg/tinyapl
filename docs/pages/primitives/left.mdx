---
glyph: '⊣'
pattern: 'r←[x]⊣y'
name: Left
---

If `x` is provided, `r` is `x`; otherwise `r` is `y`.

|Monad|Dyad|
|:----|:---|
|<combinator key="same" name="Same" />|<combinator key="left" name="Left" />|