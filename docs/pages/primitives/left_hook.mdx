---
glyph: '⊸'
pattern: 'r←[x](F⊸G)y'
name: Left Hook
---

If `x` is not provided, `r` is `(F y) G y`; if `x` is provided, `r` is `(F x) G y`.

|Monad|Dyad|
|:----|:---|
|<combinator key="left_hook" name="Left Hook" />|<combinator key="before" name="Before" />|