---
glyph: '×'
pattern: 'r←×y'
name: Case
---

`r` is the scalar application of the case function, which returns `1` for uppercase characters, `¯1` for lowercase characters and `0` for all other characters.