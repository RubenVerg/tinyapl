---
glyph: '⊆'
pattern: 'r←x⊆y'
name: Partition
---

`x` is a vector of naturals, with length equal to the tally of `y`. `r` is a nested vector containing major cells of `y`, in groups marked by consecutive equal values of `x`, dropping ones that corerspond to a zero.