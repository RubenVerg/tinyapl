---
glyph: '⎕Type'
pattern: 'r←⎕Type y'
name: Type
category: Core
---

On each scalar of `y`, return its *type*:

* `0` for a number;
* `1` for a character;
* `2` for a box;
* `3` for a wrap;
* `4` for an adverb wrap;
* `5` for a conjunction wrap;
* `6` for a struct.
