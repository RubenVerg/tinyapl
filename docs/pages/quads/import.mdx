---
glyph: '⎕Import'
pattern: 'r←⎕Import y'
name: Import
category: I/O
planned: true
---

Import a module. `y` is a string, either a file path or a string starting with `std:` pointing to a standard library module. The code in the module is ran in an isolated scope.